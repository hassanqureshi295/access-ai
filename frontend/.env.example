# AccessAI Frontend Environment Variables
# Copy to .env.production for production builds (or .env.local for local overrides)

# Backend API base URL
# Leave empty in development — Vite dev server proxies /chat, /login, etc. to port 8000
# Set to your deployed backend URL in production, e.g. https://api.yourdomain.com
VITE_API_URL=
