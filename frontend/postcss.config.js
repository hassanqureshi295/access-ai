/**
 * PostCSS configuration for AccessAI.
 *
 * Processes TailwindCSS directives and adds vendor prefixes
 * for cross-browser compatibility.
 */
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
